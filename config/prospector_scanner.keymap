// Prospector Scanner Keymap
#include <behaviors.dtsi>
#include <dt-bindings/zmk/keys.h>

/ {
    keymap {
        compatible = "zmk,keymap";
        
        default_layer {
            // This is a scanner device, no keys needed
            bindings = <>;
        };
    };
};
